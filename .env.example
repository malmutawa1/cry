# Point the frontend at a running Pressd backend to enable real auth + persistence.
# Leave unset (the default) to run fully local — e.g. the static GitHub Pages demo.
# VITE_API_URL=http://localhost:4000
